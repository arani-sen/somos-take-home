input BookInput {
  title: String!
  authorID: Int!
  """
  This is the string to the url where the image is stored like in S3
  """
  coverImageURL: String
  publishedDate: String
}

input AuthorInput {
  firstName: String!
  lastName: String!
  birthDate: String
}
