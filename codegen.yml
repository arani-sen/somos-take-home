overwrite: true
schema:
  - src/schemas/inputs.graphql
  - src/schemas/mutations.graphql
  - src/schemas/queries.graphql
  - src/schemas/types.graphql
generates:
  src/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-resolvers
